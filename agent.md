# 🧱 Bricks Land Management — AI Agent Guidelines & System Architecture (`agent.md`)

এই ডকুমেন্টটি **Bricks Land Management (ইট ভাটা ব্যবস্থাপনা সফটওয়্যার)** প্রজেক্টের কেন্দ্রীয় সংবিধান, আর্কিটেকচার ম্যানুয়াল এবং এআই এজেন্টদের জন্য বাধ্যতামূলক গাইডলাইন। প্রজেক্টে যেকোনো নতুন কাজ, বাগ ফিক্স বা ফিচার ডেভেলপমেন্ট শুরু করার আগে এই ডকুমেন্টের প্রতিটি ধারা পড়া এবং কঠোরভাবে অনুসরণ করা বাধ্যতামূলক।

---

## ১. মূল নীতি ও দায়িত্ব (Core Principles & Responsibilities)

1. **জিরো ব্রেকিং চেঞ্জ পলিসি (Zero Breaking Change):** প্রজেক্টের বর্তমান ফাংশনালিটি, ডার্ক/লাইট থিম, ডাটাবেজ লজিক, ফর্ম সাবমিশন, প্রিন্ট ফরম্যাট বা ক্যালকুলেশন যেন কোনোভাবেই নষ্ট না হয়।
2. **সর্বদা পূর্বে স্ক্যান:** কোনো ফাইলে কোড পরিবর্তনের আগে ফাইলটির আর্কিটেকচার, ইভেন্ট লিসেনার এবং রিলেটেড ফাইলগুলো ভালোভাবে পর্যালোচনা করতে হবে।
3. **ক্লিন কোড ও এররমুক্ত কনসোল:** প্রতিটি জাভাস্ক্রিপ্ট, লাইভওয়্যার মেথড ও ব্লেড ভিউ এমনভাবে লিখতে হবে যাতে ব্রাউজার কনসোলে বা সার্ভার লগে বিন্দুমাত্র এরর বা ওয়ার্নিং না আসে।
4. **ডকুমেন্টেশন আপডেট নিয়ম:** প্রজেক্টে নতুন কোনো গ্লোবাল প্যাটার্ন বা নিয়ম যুক্ত হলে সবার আগে এই `agent.md` ফাইলটি আপডেট করতে হবে।

---

## ২. প্রযুক্তিগত স্ট্যাক (Technology Stack)

- **ব্যাকএন্ড ফ্রেমওয়ার্ক:** Laravel 11/12 (PHP 8.3+)
- **ফুল-স্ট্যাক রিঅ্যাক্টিভ ফ্রেমওয়ার্ক:** Livewire 3/4 (Single Page Application - SPA Mode)
- **ফ্রন্টএন্ড ইন্টারঅ্যাক্টিভিটি:** Alpine.js v3 (ড্রপডাউন, মোডাল, টুলটিপ ও লোকাল স্টেট)
- **সিএসএস ও ডিজাইন সিস্টেম:** Tailwind CSS v4 (`@theme` সেন্ট্রালাইজড ভেরিয়েবল আর্কিটেকচার)
- **অ্যাসেট বান্ডলার:** Vite 8 (`laravel-vite-plugin`, PostCSS)
- **ডাটাবেজ ইঞ্জিন:** MySQL (Eloquent ORM, ইগার লোডিং ও ইনডেক্সড কুয়েরি)
- **প্লাগিন ও লাইব্রেরি:**
  - **Flatpickr:** কাস্টম বাংলা লোকেল ও `monthSelect` প্লাগিন সহ ডেটপিকার
  - **Chart.js:** ড্যাশবোর্ড ও রিপোর্ট অ্যানালিটিক্স চার্ট
  - **Spatie Laravel Permission:** রোল ও পারমিশন কন্ট্রোল

---

## ৩. প্রজেক্টের কমন মডিউল ও ফিচারসমূহ (Common Modules & Features)

### ৩.১ চালান মডিউল (Challan Management)
- **আজকের চালান (`challan.today`):** দৈনিক চালানের হিসাব ও এন্ট্রি।
- **অগ্রিম চালান (`challan.pending`):** অগ্রিম বুকিং চালানের ট্র্যাকিং।
- **সব চালান (`challan.all`):** ফিল্টারিং ও সার্চসহ সকল চালানের সমন্বিত লিস্ট ও প্রিন্ট।
- **কাস্টমার প্রোফাইল (`challan.customer-profile`):** নির্দিষ্ট কাস্টমারের ব্যক্তিগত চালান ও পেমেন্ট হিস্ট্রি।

### ৩.২ ডেলিভারি মডিউল (Delivery Management)
- **আজকের ডেলিভারি (`delivery.today`):** দৈনিক ডেলিভারি হিসাব।
- **আজ ডেলিভারি যাবে (`delivery.pending`):** নির্ধারিত পেন্ডিং ডেলিভারি শিডিউল।
- **বাকি ডেলিভারি লিস্ট (`delivery.all`):** সামগ্রিক অপূর্ণ ডেলিভারি রেকর্ড।

### ৩.৩ বাকি খাতা মডিউল (Due Ledger Management)
- **আজকের জমা (`due-ledger.today`):** দৈনিক বাকি কালেকশন।
- **আজ জমা দেবে (`due-ledger.due-today`):** আজকের কালেকশন টার্গেট ও ফলোআপ।
- **সব বাকি লিস্ট (`due-ledger.all-due`):** সার্বিক বকেয়া খতিয়ান।

### ৩.৪ হিসাব ও ব্যাংকিং মডিউল (Accounts & Ledger)
- **পেমেন্ট খাতা (`payment-khata`):** সামগ্রিক খরচ ও পেমেন্ট রেকর্ড।
- **খতিয়ান (`khotian`):** লেজার ভিত্তিক লেজার বুক ও বিস্তারিত অ্যাকাউন্ট স্টেটমেন্ট।
- **ক্যাশ খাতা (`cash-khata`):** ক্যাশ ইন/আউট হিসাব।
- **দেনা-পাওনা (`deuna-pauna`):** সাপ্লায়ার ও মহাজনদের দেনা-পাওনা খতিয়ান।
- **ইনভেস্টমেন্ট (`investment`):** পার্টনার/ইনভেস্টরদের বিনিয়োগ ও উইথড্রয়াল হিসাব।

### ৩.৫ ভাটা ও স্টক অপারেশন (Kiln & Material Stock)
- **লোড খাতা (`load-khata`):** কাঁচা ইট ভাটায় তোলার হিসাব (রাউন্ড ভিত্তিক)।
- **আনলোড খাতা (`unload-khata`):** পোড়ানো ইট ভাটা থেকে নামানোর হিসাব।
- **স্টক খাতা (`stock-khata`):** ক্যাটাগরি ভিত্তিক প্রস্তুত ইটের স্টক ট্র্যাকিং।
- **মালামাল স্টক (`malamal-stock`):** কয়লা, মাটি, বালু ও অন্যান্য কাঁচামালের ইনভেন্টরি।

### ৩.৬ ট্রান্সপোর্ট ও লজিস্টিকস (Transport & Fleet)
- **গাড়ির হিসাব (`vehicle-account`):** নিজস্ব গাড়ি ট্রিপ, খরচ ও মেইনটেন্যান্স।
- **গাড়ি ভাড়া (`vehicle-rent`):** ভাড়া গাড়ির ট্রিপ ও বিলিং।

### ৩.৭ অপারেশনাল টুলস ও সিস্টেম (Tools & Administration)
- **টাস্ক ম্যানেজার (`task-manager`):** অফিসিয়াল কাজের টাস্ক ও প্রগ্রেস ট্র্যাকিং।
- **ডকুমেন্টস (`documents`):** ক্যাটাগরি ভিত্তিক ফাইল/কাগজপত্র স্টোরেজ ও প্রিভিউ।
- **ফোন নাম্বার (`phone-number`):** ডিজিটাল ফোন ডিরেক্টরি।
- **এসএমএস খাতা (`sms-khata`):** বাল্ক কাস্টমার এসএমএস নোটিফিকেশন।
- **সেটিংস (`settings`):** প্রোফাইল, ইট ক্যাটাগরি ও দর নির্ধারণ, প্রিন্টার ও এসএমএস কনফিগারেশন।
- **লগইন ও আপডেট রেকর্ড (`login-history`, `update-history`):** নিরাপত্তা অডিট লগ।

---

## ৪. ডিজাইন সিস্টেম ও Tailwind CSS v4 `@theme` আর্কিটেকচার

প্রজেক্টের সমস্ত কালার ভেরিয়েবল `resources/css/app.css` ফাইলের `@theme` ব্লকে সেন্ট্রালাইজ করা:

```css
@theme {
    /* ফন্ট ফ্যামিলি */
    --font-sans: "Hind Siliguri", ui-sans-serif, system-ui, sans-serif;
    
    /* প্রাইমারি ব্রিকস গ্রিন কালার */
    --color-primary: #034c3c;         /* deep emerald */
    --color-primary-dark: #023e31;    /* dark green */
    --color-primary-light: #056651;   /* light emerald */
    --color-secondary: #068a6b;       /* active item */
    --color-accent: #009e74;          /* interactive green */

    /* ডার্ক মোড কালার */
    --color-dark-bg: #0b1329;
    --color-dark-surface: #0f172a;
    --color-dark-card: #1e293b;
    --color-dark-border: #334155;

    /* প্যালেট ম্যাপিং (Red, Emerald, Slate, Gray, Amber, Orange, Blue) */
    --color-emerald-50: #ecfdf5; ... --color-emerald-950: #022c22;
    --color-red-50: #fef2f2;     ... --color-red-950: #450a0a;
    --color-slate-50: #f8fafc;   ... --color-slate-950: #020617;
}
```

### ডিজাইন স্ট্যান্ডার্ড:
1. **টাইপোগ্রাফি:** বাংলা ও সাধারণ টেক্সটে `font-sans` ('Hind Siliguri') এবং হেডারে `font-bold` বা `font-black`।
2. **কার্ড ও মোডাল:** বর্ডার রেডিয়াস `rounded-2xl` বা `rounded-3xl`, ডার্ক মোডে `bg-slate-900 border-slate-800`।
3. **ইনপুট কন্ট্রোল:** ফোকাস অবস্থায় `focus:border-primary focus:ring-4 focus:ring-primary/10`।
4. **বাংলা সংখ্যা রূপান্তর:** সর্বদা `toBanglaNum($number)` বা JS-এ `toBnNum()` হেল্পার ব্যবহার করতে হবে।

---

## ৫. Livewire 3 SPA ও Zero-Reload নেভিগেশনাল রুলস

1. **`wire:navigate` বাধ্যতামূলক:** সাইডবার, টপবার ও অ্যাপ্লিকেশনের সমস্ত অভ্যন্তরীণ লিংকে `<a href="..." wire:navigate>` ব্যবহার করতে হবে (এক্সটার্নাল বা ফাইল ডাউনলোড ছাড়া)।
2. **জাভাস্ক্রিপ্ট সেন্ট্রালাইজেশন:** কোনো ব্লেড ভিউয়ের বডিতে ইনলাইন স্ক্রিপ্ট রাখা যাবে না। সকল গ্লোবাল JS লজিক `resources/js/app.js` ফাইলে থাকবে এবং Vite দিয়ে বান্ডেল হবে।
3. **স্ক্রিপ্ট ও স্টাইল ট্যাগ রুলস:**
   - `<head>`-এ থাকা স্ক্রিপ্ট/স্টাইলে `data-navigate-track` নিশ্চিত করতে হবে।
   - ব্লেডে কোনো অনিবার্য ইনলাইন স্ক্রিপ্ট থাকলে অবশ্যই `<script data-navigate-once>` ব্যবহার করতে হবে।
   - `<head>` সেকশনে কখনোই কোনো ইনলাইন `<style>` রাখা যাবে না (DOM diffing reload রোধে)।
4. **লাইফসাইকেল ইভেন্ট হ্যান্ডলিং:**
   ```javascript
   document.addEventListener('livewire:navigated', () => {
       window.initFlatpickrs(); // পেজ চেঞ্জে সেফলি কম্পোনেন্ট রি-ইনিশিয়ালাইজ
   });
   ```

---

## ৬. ব্যাকএন্ড ও ডাটাবেজ স্কেলাবিলিটি গাইডলাইন (Millions of Records)

1. **ইগার লোডিং (Eager Loading):** রিলেশনশিপযুক্ত টেবিলে কখনোই N+1 কুয়েরি করা যাবে না। সর্বদা `with(['items', 'customer'])` ব্যবহার করতে হবে।
2. **ডাটাবেজ লেভেল ক্যালকুলেশন:** লাখ লাখ রেকর্ডের ক্ষেত্রে পিএইচপি কালেকশন মেমোরিতে লোড না করে সরাসরি SQL এগ্রিগেশন করতে হবে:
   - ✅ **সঠিক:** `(clone $query)->sum('due')` বা `(clone $query)->count()`
   - ❌ **ভুল:** `$query->get()->sum('due')`
3. **অর্থবছর/মৌসুম (Season) ফিল্টারিং:** প্রতিটি বাণিজ্যিক কুয়েরিতে একটিভ সিজন ফিল্টার থাকা আবশ্যক:
   ```php
   $activeSeason = Setting::get('season', '২৫-২৬');
   $query->where(function($q) use ($activeSeason) {
       $q->where('season', $activeSeason)->orWhereNull('season');
   });
   ```
4. **প্যাজিনেশন:** টেবিলে সবসময় `$query->paginate($this->perPage)` ব্যবহার করতে হবে।

---

## ৭. ইউনিভার্সাল প্রিন্টিং সিস্টেম (Print System Engine)

প্রজেক্টে প্রিন্টের জন্য একটি সেন্ট্রালাইজড জাভাস্ক্রিপ্ট ইঞ্জিন রয়েছে (`window.printChallanArea`):
- **টার্গেট কন্টেইনারসমূহ:**
  - `#print-a4-customer`: A4 পোর্ট্রেট (কাস্টমার কপি)
  - `#print-a4-dual`: A4 ল্যান্ডস্কেপ (অফিস ও কাস্টমার কপি পাশাপাশি)
  - `#print-pos-customer`: 80mm পিওএস থার্মাল রিসিপ্ট
  - `#print-pos-dual`: 80mm ডুয়াল রিসিপ্ট
  - `#khotian-print-area`: A4 খতিয়ান স্টেটমেন্ট
- **প্রিন্ট কম্পোনেন্ট:** `<x-print-layout type="..." :data="..." />`

---

## ৮. কমান্ড চিটশিট (Development Commands)

```bash
# এসেট ডেভেলপমেন্ট সার্ভার
npm run dev

# প্রোডাকশন এসেট বিল্ড (CSS/JS আপডেট হলে বাধ্যতামূলক)
npx vite build

# ব্যাকএন্ড লোকাল সার্ভার
php artisan serve

# ক্যাশ ক্লিয়ারিং
php artisan view:clear
php artisan route:clear
php artisan config:clear
```
