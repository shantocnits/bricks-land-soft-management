# AI Agent Guidelines & System Instructions (`agent.md`)

এই ডকুমেন্টটি Bricks Land Management Dashboard প্রজেক্টে কর্মরত যেকোনো AI এজেন্টের জন্য একটি নির্দেশিকা বা সংবিধান হিসেবে কাজ করবে। এজেন্টের যেকোনো কাজ শুরু করার আগে এই নির্দেশিকাগুলো পড়া এবং প্রতিটি নিয়ম কঠোরভাবে মেনে চলা আবশ্যক।

---

## ১. মূল দায়িত্ব (Core Responsibility)
* **বিদ্যমান সিস্টেম বজায় রাখা:** প্রজেক্টের বর্তমান ফাংশনালিটি সম্পূর্ণ ঠিক রেখে শুধু ব্যবহারকারীর নির্দেশিত নির্দিষ্ট পরিবর্তনগুলো করতে হবে।
* **সিস্টেম ইন্টিগ্রিটি:** নতুন কোনো ফিচার যোগ বা পরিবর্তনের কারণে প্রজেক্টের বিদ্যমান কোনো ফিচার বা সিস্টেম যেন কোনোভাবেই নষ্ট না হয়। কোডে হাত দেওয়ার আগে পূর্ববর্তী আর্কিটেকচার ভালোভাবে পর্যালোচনা করতে হবে।

---

## ২. ডিজাইন সিস্টেম ও গাইডলাইন (Design System Guidelines)
প্রজেক্টের ভিজ্যুয়াল হুবহু বজায় রাখতে হবে। ব্যবহারকারীর লিখিত অনুমতি ছাড়া ডিজাইনে কোনো পরিবর্তন আনা যাবে না।

### ক. টাইপোগ্রাফি (Typography)
* **ফন্ট ফ্যামিলি:** বাংলা টেক্সটের জন্য মূলত **'Hind Siliguri'** এবং ইংরেজি ও ইন্টারফেসের জন্য **'Instrument Sans'** ফন্ট ব্যবহৃত হয়।
* **ফন্ট স্টাইল:** হেডিংস এবং টাইটেল সবসময় বোল্ড (`font-bold` বা `font-extrabold`) হবে।

### খ. কালার প্যালেট (Color Palette)
প্রজেক্টটি একটি কাস্টম গ্রিন থিমের উপর ভিত্তি করে তৈরি। কালার ভেরিয়েবলগুলো নিম্নরূপ:
* **Primary Color (সবুজের শেড):** `#034C3C` (ডিইপ গ্রিন) - ড্যাশবোর্ড কার্ড, টুলটিপস ও বাটন ব্যাকগ্রাউন্ড।
* **Primary Dark:** `#023E31` (গাঢ় সবুজ) - সাইডবার লোগো সেকশন ও হেডার।
* **Primary Light:** `#056651` - হোভার স্টেট।
* **Secondary Color:** `#068A6B` (অ্যাক্টিভ মেনু ব্যাকগ্রাউন্ড)।
* **Accent Color:** `#009E74` (লগইন থিম কালার)।
* **Dark/Light Mode Colors:** 
  * লাইট মোডে ব্যাকগ্রাউন্ডের জন্য `bg-white` এবং `bg-gray-50` ব্যবহৃত হবে। টেক্সট হবে `text-gray-800` বা `text-slate-700`।
  * ডার্ক মোডে ব্যাকগ্রাউন্ডের জন্য `bg-slate-900` এবং `bg-slate-950` ব্যবহৃত হবে। টেক্সট হবে `text-white` এবং `text-slate-350`।

### গ. কম্পোনেন্ট ডিজাইন (Component Styling)
* **বাটন:** বাটনগুলো সাধারণত `rounded-xl` বা `rounded-2xl` বর্ডার রেডিয়াস যুক্ত এবং এতে শ্যাডো ইফেক্ট থাকবে।
* **ইনপুট ফিল্ড:** ইনপুট ফিল্ডের বর্ডার হবে হালকা ধূসর। ফোকাস করলে বর্ডার কালার অবশ্যই প্রাইমারি গ্রিন কালার হবে এবং হালকা রিং ইফেক্ট থাকবে (`focus:border-primary focus:ring-4 focus:ring-primary/10`)।

---

## ৩. লেআউট ও রেসপনসিভনেস (Layout & Responsiveness)
* **প্যাডিং ও মার্জিন:** প্রতিটি সেকশন, পেজ এবং কার্ডের প্যাডিং ও মার্জিন প্রজেক্টের স্ট্যান্ডার্ড অনুযায়ী সুষম হতে হবে।
* **১০০% রেসপন্সিভ ডিজাইন:** প্রতিটি পরিবর্তন ডেক্সটপ (`Desktop`) এবং মোবাইল (`Mobile`) উভয় ভিউতেই পিক্সেল-পারফেক্ট হতে হবে। সাইডবার মোবাইলে অফক্যানভাস হিসেবে স্লাইড করবে এবং ডেক্সটপে কোল্যাপ্সড/এক্সপ্যান্ড হবে।

---

## ৪. ডার্ক/লাইট মোড (Dark/Light Mode)
* **গ্লোবাল থিম স্টেট:** অ্যাপ্লিকেশনের ডার্ক/লাইট মোড টগল সবসময় গ্লোবাল `darkMode` স্টেট অনুযায়ী কাজ করবে।
* **এলিমেন্ট সিঙ্ক:** প্রতিটি নতুন কম্পোনেন্ট বা ইন্টারফেস পরিবর্তন করার সময় ডার্ক এবং লাইট উভয় থিমেই তার কালার ও কনট্রাস্ট চেক করতে হবে যাতে টেক্সট বা ব্যাকগ্রাউন্ড পড়তে কোনো সমস্যা না হয়।

---

## ৫. আপডেট রুল (Update Rule)
* **গাইডলাইন আপডেট বাধ্যতামূলক:** ভবিষ্যতে যদি প্রজেক্টের নতুন কোনো ডিজাইন গাইডলাইন বা পরিবর্তন আসে (যেমন: ফন্ট ফ্যামিলি পরিবর্তন, নতুন বাটন স্টাইল ইত্যাদি), তবে **যেকোনো পরিবর্তনের আগে এই `agent.md` ফাইলটি প্রথম ধাপে আপডেট করতে হবে**। এর পরই কেবল মূল কোড ফাইলে কাজ শুরু করা যাবে।

---

## ৬. কোডিং স্ট্যান্ডার্ড (Coding Standards)
* **মডুলার স্ট্রাকচার:** কোড সবসময় মডুলার হতে হবে (যেমন: Livewire-এর আলাদা আলাদা কম্পোনেন্ট এবং ব্লেড টেমপ্লেট পারশিয়ালস)।
* **ক্লিন কোড ও কমেন্টস:** প্রতিটি বড় পরিবর্তনের পেছনে যুক্তি কমেন্টের মাধ্যমে লিখে রাখতে হবে।
* **কনসোল এররমুক্ত কোড:** প্রজেক্টের যেকোনো জাভাস্ক্রিপ্ট বা আলপাইন জেএস কোড এমনভাবে লিখতে হবে যাতে ব্রাউজারের ডেভ-টুলস কনসোলে কোনো প্রকার এরর না আসে।
